#include <stdio.h>
#include <string.h>

//String Functions


int main(){

    char string1[] = "Dolla";
    char string2[] = "Man";

    //strlwr(string1);                      //lowercase
    //strupr(string1);                      //uppercase
    //strcat(string1, string2);             //appends string2 to the end of string1
    //strncat(string1, string2, n);         //appends n number of characters in string2 to string1
    //strcpy(string1, string2);             //copy string2 to string1
    //strncpy(string1, string2, n);         //copies n number of characters of string2 to string1
    
    //strset(string1, 'A');                 //sets characters from string1 to A
    //strnset(string1, 'A', n);             //sets n characters in string1 to A
    //strrev(string1');                     //reverses string

    //int result = strlen(string1);                 //returns the string length
    //int result = strcmp(string1, string2);        //compares all chracters within 2 strings
    //int result = strncmp(string1, string2, n);    //compares n chracters within 2 strings
    //int result = strcmpi(string1, string2);       //same thing as strcmp, just not case sensitive
    //int result = strnicmp(string1, string2, n);   //same thing as strncmp, just not case sensitive
    




    printf("%s", string1);

    return 0;
}